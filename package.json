{
  "name": "worker",
  "version": "1.0.0",
  "description": "Reads votes from Redis and stores them in Postgres",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js"
  },
  "dependencies": {
    "ioredis": "^5.4.1",
    "pg": "^8.13.1"
  }
}